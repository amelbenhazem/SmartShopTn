# OWASP ZAP Baseline Scan Configuration
# This file contains rules for the ZAP baseline scan

# Exclude specific paths from scanning
# Example:
# exclude = ^https?://localhost:5000/api/admin.*

# Alert Filters
# You can suppress specific alerts by rule ID
# Example:
# 10202 IGNORE (Absence of Anti-CSRF Tokens)

# Confidence levels: 0=False Positive, 1=Low, 2=Medium, 3=High
# Risk levels: 0=Info, 1=Low, 2=Medium, 3=High

# Custom rules for your application
# Uncomment and modify as needed

# Ignore static assets
# exclude = .*\.(jpg|jpeg|png|gif|css|js|svg|ico)$

# Authentication - Add your auth configuration here if needed
